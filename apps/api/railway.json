{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && pnpm --filter @savicol/api prisma:generate && pnpm --filter @savicol/api build"
  },
  "deploy": {
    "startCommand": "pnpm --filter @savicol/api start",
    "healthcheckPath": "/api/v1/auth/login",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
