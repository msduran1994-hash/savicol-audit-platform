{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && cd apps/api && sh prisma/postgres-prepare.sh && pnpm build"
  },
  "deploy": {
    "startCommand": "npx prisma db push --accept-data-loss --skip-generate && npx prisma db seed && node dist/main",
    "healthcheckPath": "/api/v1/auth/login",
    "healthcheckTimeout": 60,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
